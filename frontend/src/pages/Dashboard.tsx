/**
 * Dashboard Page - Uses PortfolioDashboard with tabbed interface
 * Replaces single-page layout with professional 5-tab design
 */
import PortfolioDashboard from '@/components/portfolio/PortfolioDashboard';

const Dashboard = () => {
  return <PortfolioDashboard />;
};

export default Dashboard;
